{% extends 'base.html.twig' %}

{% block title %}vos articles{% endblock %}

{% block body %}

    <div class="articles">
        {% for post in posts %}
            <div class="article">
                {{ post.title }}
                <a href="{{ path('post_show', {'id': post.id}) }}" class="btn-primary">Lire la suite</a>
            </div>
        {% endfor %}
    </div>
    <a href="{{ path('post_create') }}" class="btn-primary">Nouveau post</a>

{% endblock %}
